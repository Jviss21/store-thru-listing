# Demo access password for customer sharing (server-side only).
# Default if unset: testgoodwill
DEMO_PASSWORD=testgoodwill

# Optional: set in Vercel Production / Preview environments.
# Do NOT prefix with NEXT_PUBLIC_ — the password must stay server-side.
